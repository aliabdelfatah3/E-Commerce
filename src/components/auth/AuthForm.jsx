import React from "react";

function AuthForm() {
  return (
    // if login and register diverge, split into LoginForm.jsx and RegisterForm.jsx.
    <div>AuthForm</div>
  );
}

export default AuthForm;
